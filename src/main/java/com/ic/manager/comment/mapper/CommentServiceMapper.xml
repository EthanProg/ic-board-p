<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ic.manager.advertise.mapper.AdvertiseServiceMapper">

    <select id="queryCommentList" resultType="map" parameterType="map">
        select * from ic_comment_ad
    </select>

    <select id="queryComment" resultType="map" parameterType="map">

    </select>

    <insert id="addComment" parameterType="map">

    </insert>

    <update id="updateComment" parameterType="map">

    </update>

    <delete id="deleteComment" parameterType="map">

    </delete>

</mapper>
